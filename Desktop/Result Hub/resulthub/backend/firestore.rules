rules_version = '2';
service cloud.firestore {
  // All data is managed by the backend through the Firebase Admin SDK,
  // which bypasses these rules. The browser never reads or writes Firestore,
  // so client access is denied entirely.
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
